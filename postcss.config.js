module.exports = {
  plugins: [
    require('autoprefixer')(),
    // require('postcss-import')(),
    // require('postcss-simple-vars')(),
    // require('cssnano')({
    //   autoprefixer: false
    // })
  ]
}